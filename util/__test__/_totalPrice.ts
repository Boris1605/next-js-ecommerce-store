// import { expect, test } from '@jest/globals';

// import totalPrice from '../../app/cart/totalPrice';

// test('add subtotal of each cake to get final total', () => {
//   const product1 = { id: 1, name: 'name1', price: 10, quantity: 1 };
//   const product2 = { id: 2, name: 'name2', price: 20, quantity: 2 };
//   const order = [product1, product2];

//   const total = totalPrice(order);

//   expect(total).toBe(50);
// });

// test('Show total of 0 if order is empty', () => {
//   const order = [];
//   const total = totalPrice(order);

//   expect(total).toBe('0');
// });
